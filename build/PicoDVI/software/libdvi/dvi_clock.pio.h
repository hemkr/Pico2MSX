// ---------------------------------------------------------------- //
// This file is autogenerated by pioasm version 2.2.0; do not edit! //
// ---------------------------------------------------------------- //

#pragma once

#if !PICO_NO_HARDWARE
#include "hardware/pio.h"
#endif

// --------- //
// dvi_clock //
// --------- //

#define dvi_clock_wrap_target 0
#define dvi_clock_wrap 1
#define dvi_clock_pio_version 1

static const uint16_t dvi_clock_program_instructions[] = {
            //     .wrap_target
    0xe402, //  0: set    pins, 2                [4]
    0xe401, //  1: set    pins, 1                [4]
            //     .wrap
};

#if !PICO_NO_HARDWARE
static const struct pio_program dvi_clock_program = {
    .instructions = dvi_clock_program_instructions,
    .length = 2,
    .origin = -1,
    .pio_version = dvi_clock_pio_version,
#if PICO_PIO_VERSION > 0
    .used_gpio_ranges = 0x0
#endif
};

static inline pio_sm_config dvi_clock_program_get_default_config(uint offset) {
    pio_sm_config c = pio_get_default_sm_config();
    sm_config_set_wrap(&c, offset + dvi_clock_wrap_target, offset + dvi_clock_wrap);
    return c;
}

// sets up the GPIO output, and configures the SM to output on particular pins
void dvi_clock_program_init(PIO pio, uint sm, uint offset, uint pin) {
   pio_gpio_init(pio, pin);
   pio_gpio_init(pio, pin+1);
   pio_sm_set_consecutive_pindirs(pio, sm, pin, 2, true);
   pio_sm_config c = dvi_clock_program_get_default_config(offset);
   sm_config_set_set_pins(&c, pin, 2);
   pio_sm_init(pio, sm, offset, &c);
}

#endif

